pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git(
                    url: "${params.GIT_REPOSITORY}"
                    branch: "${params.GIT_BRANCH}"
                    credentialsId: "${params.GIT_CREDENTIALS}"
                ) 
            }
        }
    }
}